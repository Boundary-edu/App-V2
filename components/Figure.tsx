export const Figure = ({ children }: { children: React.ReactNode }) => {
    return (
      <div 
        className={
          `text-xl text-black`
        }
      >
        {children}
      </div>
    )
  };
  